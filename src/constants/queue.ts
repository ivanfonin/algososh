import { ElementStates } from "../types/element-states";
import { TQueueItem } from "../types/queue";

export const QUEUE_SIZE = 7;

export const QUEUE_ITEM: TQueueItem = {
  state: ElementStates.Default,
  letter: "",
};
